import BookListPage from './BookListPage';

// The public-facing AllBooksPage (for customers) is implemented in BookListPage.
// We keep this file to preserve imports and routes that reference AllBooksPage.
export default BookListPage;